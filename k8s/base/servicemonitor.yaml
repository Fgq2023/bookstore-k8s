apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bookstore-backend-metrics
  namespace: bookstore
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: bookstore-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
